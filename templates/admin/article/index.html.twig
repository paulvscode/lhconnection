{% extends 'base.html.twig' %}

{% block title %}Edition Article{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h2 class="p-2">Gérer les Articles </h2>

        <table class="table table-striped">
            <thead>
            <tr>
                <th>Titre</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for article in articles %}
                <tr>
                    <td class="align-middle">{{ article.title }}</td>
                    <td>
                        <a href="{{ path('admin_article_edit', {id: article.id}) }}"
                           class="btn btn-secondary">Editer</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        <a class="btn btn-primary" href="{{ path('admin_index') }}">Retour</a>
        <a class="btn btn-success" href="{{ path('admin_article_create') }}">Ajouter un article</a>
    </div>
{% endblock %}
