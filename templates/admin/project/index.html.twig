{% extends 'base.html.twig' %}

{% block title %}Edition Projets{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h2 class="p-2">Gérer les projets </h2>

        <table class="table table-striped">
            <thead>
            <tr>
                <th>Titre</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for project in projects %}
                <tr>
                    <td class="align-middle">{{ project.title }}</td>
                    <td>
                        <a href="{{ path('admin_project_edit', {id: project.id}) }}"
                           class="btn btn-secondary">Editer</a>
                        <a href="{{ path('admin_project_delete', {id: project.id}) }}"
                           class="btn btn-danger">Supprimer</a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <a class="btn btn-primary" href="{{ path('admin_index') }}">Retour</a>
        <a class="btn btn-success" href="{{ path('admin_project_create') }}">Ajouter un projet</a>
    </div>
{% endblock %}
